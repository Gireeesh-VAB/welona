export default function HomePage() {
  return (
    <main>
      <h1>Welona Admin — Backend API</h1>
      <p>
        This service exposes <code>/api/v1/*</code>. The Admin Panel UI runs
        separately (default <code>http://localhost:3001</code>).
      </p>
    </main>
  );
}
